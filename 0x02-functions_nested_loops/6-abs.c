#include "main.h"

/**
 *  _abs - this is the function of the task
 *  @a: - this is our function  parameter
 *  Return: -a or a;
 */
int _abs(int a)
{
if (a < 0)
return (-a);
else if (a >= 0)
{
return (a);
}
return (0);
}
